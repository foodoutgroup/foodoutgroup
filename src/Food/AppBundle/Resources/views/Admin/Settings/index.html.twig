{% extends '@FoodApp/admin_layout.html.twig' %}

{% block title %}{% endblock %}

{% block sonata_admin_content %}
    <div class="container">
        <div class="col-lg-12">
            <div class="page-header">
                <h1>
                    {% trans %}Settings{% endtrans %}<small>{% trans %}Change base system settings{% endtrans %}</small>
                </h1>
            </div>
        </div>
    {{ form_start(form) }}
        <div class="col-lg-6">
            {{ form_widget(form) }}
        </div>
    {{ form_end(form) }}
    </div>
{% endblock %}